# Gemini — lee primero

Antes de tocar cualquier `*BrandHub.svelte` o `*PremiumLanding.svelte` (Jeep, Fiat, Ram,
Dodge, Peugeot), lee **`AGENTS.md`** en la raíz del repo. Ahí está el patrón obligatorio,
los errores que ya se cometieron dos veces (no los repitas), y el estado actual de cada
marca. Es la bitácora compartida entre agentes — actualízala si haces cambios relevantes.
